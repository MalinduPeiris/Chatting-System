# Chatting-System
The server runs continuously and listens for incoming client connections. Each connected client runs in its own thread, enabling smooth multi-user communication. When a user sends a message, the server broadcasts it to all connected clients in real-time.
